@model IEnumerable<MoneyMe.Web.Models.Scaffolding.Position>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            Accounts
        </th>
        <th>
            Users
        </th>
        <th>
            UserPositions
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
        <td>
			@(item.Accounts == null ? "None" : item.Accounts.Count.ToString())
        </td>
        <td>
			@(item.Users == null ? "None" : item.Users.Count.ToString())
        </td>
        <td>
			@(item.UserPositions == null ? "None" : item.UserPositions.Count.ToString())
        </td>
    </tr>
}

</table>


