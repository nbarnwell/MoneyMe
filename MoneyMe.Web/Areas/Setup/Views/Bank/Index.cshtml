@model IEnumerable<MoneyMe.Web.Models.Scaffolding.Bank>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            Name
        </th>
        <th>
            BankAccounts
        </th>
        <th>
            BankSpecificTransactionTypes
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
        <td>
			@item.Name
        </td>
        <td>
			@(item.BankAccounts == null ? "None" : item.BankAccounts.Count.ToString())
        </td>
        <td>
			@(item.BankSpecificTransactionTypes == null ? "None" : item.BankSpecificTransactionTypes.Count.ToString())
        </td>
    </tr>
}

</table>


