@model MoneyMe.Web.Models.Scaffolding.Budget

@* This partial view defines form fields that will appear when creating and editing entities *@

<div class="editor-label">
    @Html.LabelFor(model => model.Name)
</div>
<div class="editor-field">
    @Html.EditorFor(model => model.Name)
    @Html.ValidationMessageFor(model => model.Name)
</div>

<div class="editor-label">
    Account
</div>
<div class="editor-field">
    @Html.DropDownListFor(model => model.AccountId, ((IEnumerable<MoneyMe.Web.Models.Scaffolding.Account>)ViewBag.PossibleAccounts).Select(option => new SelectListItem {
        Text = (option == null ? "None" : option.Name), 
        Value = option.Id.ToString(),
        Selected = (Model != null) && (option.Id == Model.AccountId)
    }), "Choose...")
    @Html.ValidationMessageFor(model => model.AccountId)
</div>
